require("blink.cmp").setup({
  keymap = {
    preset = "super-tab",
  },
  completion = {
    accept = {
      auto_brackets = { enabled = false }, -- Adjust other settings as needed
    },
  },
})

return {}
